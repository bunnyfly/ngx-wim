import { WimDirective } from './wim.directive';

describe('WimDirective', () => {
  it('should create an instance', () => {
    const directive = new WimDirective();
    expect(directive).toBeTruthy();
  });
});
